package io.github.chess2.View

import com.badlogic.gdx.graphics.g2d.SpriteBatch
import io.github.chess2.ScreenState

//TODO PlayingState. PlayingState og GameOverState???
class GameState(private val batch: SpriteBatch, private val context: StageContext) : ScreenState {//(private val batch: SpriteBatch) : ScreenState
    override fun render(delta: Float) {
    }

    override fun update(delta: Float) {
    }

    override fun dispose() {
    }
}
